/*
 * Main JS file.
 * 
 * Made by <author>.
 * Licenced under the <licence> licence.
 */

require(["plugins/default"], function() {
	// This function is called when js/plugins/default.js is loaded.
});

// Avoid `console` errors in browsers that lack a console.
require(["plugins/console"]);